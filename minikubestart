# docker starten
# Minikube starten
minikube start

# Images bauen (in Minikube)
eval $(minikube docker-env)
docker build -t api-service:latest ./api-service
docker build -t frontend:latest ./frontend
docker build -t database-service:latest ./database

# Deployments anwenden
kubectl apply -f kubernetes/

# Status prüfen
kubectl get pods
kubectl get services

# Im Browser öffnen
minikube service frontend

# port forwarding für die db tests
kubectl port-forward service/database-service 5002:5002
